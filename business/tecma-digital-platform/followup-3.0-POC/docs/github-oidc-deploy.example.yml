# Esempio — GitHub Actions: OIDC verso cloud (no chiavi long-lived)
#
# NON è un workflow attivo: copiare gli snippet nel workflow reale dopo aver
# configurato il provider (AWS IAM OIDC, Azure OIDC, GCP WIF, ecc.).
#
# Docs AWS: https://docs.github.com/en/actions/deployment/security-hardening-your-deployments/configuring-openid-connect-in-amazon-web-services
#
# name: example-oidc-deploy
# on:
#   workflow_dispatch:
#
# permissions:
#   id-token: write   # obbligatorio per OIDC
#   contents: read
#
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#
#       - name: Configure AWS (OIDC)
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: arn:aws:iam::ACCOUNT_ID:role/GitHubActionsDeployRole
#           aws-region: eu-central-1
#
#       # ... step di deploy che usano AWS CLI senza AWS_ACCESS_KEY_ID in secrets
