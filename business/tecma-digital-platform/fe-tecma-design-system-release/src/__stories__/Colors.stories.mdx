import { Meta, ColorPalette, ColorItem } from '@storybook/addon-docs';
import { colors } from '../constants/styles';
import { getColorsByKey } from '../helpers/getColorsByKey';

<Meta title="Colors" />

<ColorPalette>
  {Object.keys(colors).map((colorKey) => (
    <ColorItem title={`${colorKey}`} colors={colors[colorKey]} />
  ))}
</ColorPalette>
