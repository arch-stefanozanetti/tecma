import { Locale } from 'rc-picker/lib/interface';

const locale: Partial<Locale> = {
  locale: 'es_ES',
  yearFormat: 'YYYY',
  monthFormat: 'MMMM',
  fieldDateFormat: 'MMM dd yyyy',
  dayFormat: 'D',
  monthBeforeYear: true,
};

export default locale;
