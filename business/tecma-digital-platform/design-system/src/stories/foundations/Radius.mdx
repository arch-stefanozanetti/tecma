import { Meta, Canvas } from '@storybook/blocks';
import * as RadiusStories from './Radius.stories';

<Meta of={RadiusStories} />

# Radius

Source: `tokens/radius` · Figma: `values.border-radius` · CSS: `--tecma-values-border-radius-*`

Token border-radius semantici (external, internal, element, standard) e scala pratica (sm, md, lg).

## Default

<Canvas of={RadiusStories.Default} />

## Riferimenti

- Primitive in `data/figma-primitives-radius.json` · `npm run sync:tokens`
