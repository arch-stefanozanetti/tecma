import { Meta, Canvas } from '@storybook/blocks';
import * as ColorsStories from './Colors.stories';

<Meta of={ColorsStories} />

# Colors

Source: `tokens/color.ts` · CSS: `--tecma-color-*`

Token colore Figma alias / Tecma v2. Variabili CSS `--tecma-color-*` usate in tutto il Design System.

## Default

<Canvas of={ColorsStories.Default} />

## Riferimenti

- Token in `tokens/color.ts` e `css/tokens.css`
