import { Meta, Canvas } from '@storybook/blocks';
import * as TypographyStories from './Typography.stories';

<Meta of={TypographyStories} />

# Typography

Source: token Figma · CSS: `--tecma-typography-*`

Typeface, size standard, oversize e weight. Variabili `--tecma-typography-*` per tipografia coerente.

## Default

<Canvas of={TypographyStories.Default} />

## Riferimenti

- Token in `css/tokens.css`
