replicaCount: 2

image:
  repository: registry.example.com/followup/web
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  host: followup-dev-1.tecmasolutions.com
