# Redeploy Render (`followup-3-fe`)

Render può non avviare un nuovo build del sito statico se l’ultimo commit non tocca questa cartella (`rootDir` del servizio). In quel caso: commit minimo qui + push su `main`, oppure **Manual Deploy** dalla dashboard del servizio.
