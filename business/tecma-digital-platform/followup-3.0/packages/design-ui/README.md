# @followup/design-ui (deprecato)

Questo package **non è più usato** dall’app web: i componenti UI vivono in `apps/web/src/components/ui` (allineati al POC).

Mantenuto nel monorepo solo per riferimento storico; non aggiungere nuovi consumer.
